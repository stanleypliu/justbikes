

<div class="container">
  <div class="bicycle-form">
    <h2>Post a new bicycle listing:</h2>
    <div class="card">
      <%= simple_form_for(@bicycle) do |f| %>
        <%= f.input :title %>
        <%= f.input :style %>
        <%= f.input :size %>
        <%= f.input :price, :label => 'Price (£)' %>
        <%= f.input :location %>
        <%= f.submit 'Create posting', class: 'btn btn-primary' %>
      <% end %>
    </div>
  </div>
</div>
